<UserControl x:Class="PingWidget.Views.ServerBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="22" Margin="0,1">

    <Border Background="{Binding BackgroundBrush}" CornerRadius="3" Padding="8,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="34" />
                <ColumnDefinition Width="52" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Hostname}" Foreground="White" FontFamily="Segoe UI" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>

            <TextBlock Grid.Column="1" Text="{Binding MuteIndicator}" Foreground="#FFCC00" FontFamily="Segoe UI Emoji" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,6,0"/>

            <TextBlock Grid.Column="2" Text="{Binding LatencyLabel}" HorizontalAlignment="Left" Foreground="#AAAAAA" FontFamily="Segoe UI" FontSize="11" VerticalAlignment="Center"/>

            <TextBlock Grid.Column="3" Text="{Binding LatencyValue}" HorizontalAlignment="Right" Foreground="{Binding ValueColor}" FontFamily="Segoe UI" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>